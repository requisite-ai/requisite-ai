# Core (always required)
pydantic>=2.7
pydantic-settings>=2.3

# Providers (install only what you use)
openai>=1.35        # OpenAI provider
google-genai>=1.0   # Gemini provider (unified SDK; do NOT install google-generativeai)

# Orchestration backends (optional)
langgraph>=0.2      # only needed if you use workflow.use_langgraph()

# Dev / testing
pytest>=8.0
pytest-asyncio>=0.23
pytest-cov>=5.0
ruff>=0.5
mypy>=1.10
